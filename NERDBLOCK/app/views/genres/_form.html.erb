<%= form_with(model: @subscriptions, local: true) do |form| %>

  <%= form.select :option_id, [['Option 1',1],['Option 2',2]] %>
  <%= form.submit yield(:button_text), class: "btn btn-primary" %>

<% end %>
